export const counterexamplePrompt = [
  "Look for a small counterexample against the user's own algorithm.",
  "Do not use hidden LeetCode tests or claim access to them.",
  "If no counterexample is apparent, say what class of cases should be manually tested."
].join("\n");
